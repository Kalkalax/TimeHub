<UserControl x:Class="TiCloud_Desktop.views.content.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="677" Width="1232">
    <Grid>

        <!--Podział widoku na wiersze-->
        <Grid.RowDefinitions>
            <RowDefinition Height="435*"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="218*"/>
        </Grid.RowDefinitions>

        <!--Podział widoku na kolumny-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="To jest strona główna." 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   FontSize="20" Grid.Column="2" Margin="0,50,0,0" Height="27" Width="192" 
        />

        <!-- Dodajemy przycisk do dodoawania projektów Click="AddProjectButton_Click"-->
        <Button x:Name="AddProjectButton" 
            Content="Dodaj" 
            Margin="253,216,105,76"
            
            Grid.Column="4"
        />

        <!-- Dodajemy przycisk do usuwania projektów  Click="DeleteProjectButton_Click"-->
        <Button x:Name="DeleteProjectButton" 
            Content="Usuń" 
            Margin="42,220,342,81"
            
            Grid.Column="4"
        />

        <!-- Dodajemy liste do wyświetlania danych SelectionChanged="DataListView_SelectionChanged" -->
        <ListView x:Name="DataListView" 
                  Margin="59,23,77,44"  
                  Grid.Row="2" 
                  
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                  ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.Column="4">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Data" Width="140"  >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Date}" TextAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Czas" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Time}" TextAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock x:Name="TotalTimeTextBlock" 
            Text="Łączny czas w projekcie: 00:00:00" 
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            Margin="149,115,0,0" Grid.Column="4" Height="62" Width="174"
        />


    </Grid>
</UserControl>
